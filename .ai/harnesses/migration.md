# Create Migration Prompt

Assess a requested schema/data migration for `nodika-core`: **[change]**. This repository currently has no database library, schema, migration tool, or datastore configuration. Do not generate executable migrations until the user selects and configures a persistence boundary. First propose the minimum architecture, compatibility plan, validation, rollback, and test strategy; then implement only explicitly approved setup.
