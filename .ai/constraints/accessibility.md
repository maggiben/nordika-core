# Accessibility Constraints

- If a frontend is introduced, meet WCAG 2.2 AA expectations for semantic markup, keyboard navigation, focus management, labels, contrast, and error feedback.
- Do not add UI behavior that depends on pointer-only, color-only, or animation-only cues.
- This server-only repository has no user interface today; do not fabricate frontend requirements for API changes.
