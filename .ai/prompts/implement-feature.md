# Implement Feature Prompt

You are changing `nodika-core`, a minimal NestJS 11 API. Read `.ai/docs/repository-analysis.md`, applicable `.ai/skills/`, and `.ai/constraints/` first. Implement: **[feature]**.

Define route/input/output/errors before coding. Keep HTTP translation in controllers and use cases in services; create a domain module instead of expanding the starter root classes when appropriate. Do not invent database, auth, UI, cloud, or external integration behavior. Add unit and e2e tests, run build and relevant tests, and report changed files, contract, validation, and remaining assumptions.
