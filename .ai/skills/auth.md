# Authentication Skill

Authentication and authorization are not configured in this application. Before introducing protected routes, define the identity provider, credential lifecycle, guard strategy, authorization policy, error behavior, and tests. Never claim a route is protected without an active, tested guard.
