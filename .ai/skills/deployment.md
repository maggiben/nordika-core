# Deployment Skill

The service listens on `PORT` (default `3000`) and uses `MONGO_URI` or `MONGO_URL` for MongoDB. Configure values in a secure deployment secret store, never source control. Verify build, startup, environment validation, and rollback steps for the chosen platform before release.
