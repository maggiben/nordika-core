# CI/CD Skill

No CI/CD workflow is configured. When one is introduced, run dependency installation, build, unit tests, e2e tests, and non-mutating lint checks. Keep deployment credentials in the CI secret store and require a reviewed, reproducible release path.
