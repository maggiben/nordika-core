# Docker Skill

Docker is not configured. If containerization is required, use a minimal production image, deterministic dependency installation, a non-root runtime user, explicit environment configuration, and documented local build and run commands.
