# Git Skill

Keep commits focused and do not commit generated output, dependency caches, secrets, or `.env` files. Preserve shared history: do not force-push, reset, or rewrite published commits. Review the complete diff and relevant validation results before committing.
